# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Release distributables (installer/portable ZIP/APK/checksums) - build
# output regenerated from installer/*.wxs and the release build, not source.
/dist/
*.wixpdb

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Rust
/rust/target/
**/*.rs.bk

# Cross-compiled native engine copies staged into the Android project by
# the BUILD.md workflow - regenerate via `cargo build --target ...`, don't
# commit the binary.
/android/app/src/main/jniLibs/

# Claude Code harness internal state (not project source)
.claude/

# Python (branding/scripts/ generation pipeline)
__pycache__/
*.pyc
