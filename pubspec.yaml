name: dupora
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# On Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.0+2

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # State management: plain ChangeNotifier + provider. Chosen over Riverpod
  # for this project because the app has few, well-scoped long-lived
  # controllers (scan engine, settings) rather than a large dependency graph
  # of small providers - provider's simplicity avoids codegen and keeps the
  # architecture easy to follow. See ARCHITECTURE.md.
  provider: ^6.1.2

  # Embedded database for the hash cache. Chosen over Isar: Isar's last
  # release predates this project's Dart/Flutter SDKs and its maintenance
  # has stalled, which is a correctness/longevity risk for a persistent
  # cache that must survive SDK upgrades. Drift wraps SQLite (a
  # transactional, battle-tested, actively maintained embedded database)
  # with a type-safe Dart query layer. See ARCHITECTURE.md.
  drift: ^2.21.0
  sqlite3_flutter_libs: ^0.5.27
  sqlite3: ^2.7.4
  path_provider: ^2.1.5
  path: ^1.9.0

  # Windows volume enumeration, Recycle Bin (SHFileOperation), and other
  # Win32 calls not covered by pure-Dart/Flutter APIs.
  win32: ^5.11.0
  ffi: ^2.1.4

  # Pure-Dart image decoding for thumbnail generation; no native codec
  # dependency, works identically on every target platform.
  image: ^4.5.4

  shared_preferences: ^2.5.3
  intl: ^0.20.2
  logging: ^1.3.0
  meta: ^1.15.0
  crypto: ^3.0.6

  # Native "Add Folder" directory-picker dialog on desktop platforms.
  file_selector: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Drives the real compiled app end-to-end (real widget tree, real native
  # engine, real filesystem) rather than a widget-test harness - used for
  # the Windows production-executable smoke test. See integration_test/.
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.21.0
  build_runner: ^2.4.13
  # Used directly in tests to fake path_provider's platform channel (not
  # available under plain `flutter test`) with a real temp directory.
  path_provider_platform_interface: ^2.1.2
  plugin_platform_interface: ^2.1.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
